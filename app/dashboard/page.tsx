import { AppShell } from "@/components/app-shell";
import { ArticleCard, CaseCard, SectionHeader } from "@/components/cards";
import { articles, cases, dashboardStats } from "@/lib/data";
export default function DashboardPage(){return <AppShell><section><h1 className="text-3xl font-black text-charcoal md:text-5xl">Chào mừng trở lại, Minh Anh 👋</h1><p className="mt-3 text-warmgray">Hôm nay bạn muốn học gì mới để nâng tầm tay nghề?</p><div className="mt-8 grid gap-4 sm:grid-cols-2 xl:grid-cols-4">{dashboardStats.map((stat)=><div key={stat.label} className="rounded-3xl bg-white p-6 shadow-soft"><p className="text-sm font-bold text-warmgray">{stat.label}</p><div className="mt-3 text-4xl font-black text-charcoal">{stat.value}</div><p className="text-sm text-warmgray">{stat.sub}</p></div>)}</div></section><section className="mt-10"><SectionHeader title="Tiếp tục học"/><div className="grid gap-6 lg:grid-cols-3">{articles.slice(0,3).map((article)=><ArticleCard key={article.slug} article={article}/>)}</div></section><section className="mt-10"><SectionHeader title="Case gần đây"/><div className="grid gap-6 lg:grid-cols-3">{cases.map((item)=><CaseCard key={item.title} item={item}/>)}</div></section></AppShell>}
