import { AppShell } from "@/components/app-shell";
import { ArticleCard, SectionHeader } from "@/components/cards";
import { articles } from "@/lib/data";
export default function NotebookPage(){return <AppShell><SectionHeader title="Sổ tay của tôi" desc="Lưu giữ bài học, ghi chú kỹ thuật, checklist và nội dung quan tâm để xem lại khi cần."/><div className="mb-6 flex flex-wrap gap-3">{["Bài đã lưu","Ghi chú kỹ thuật","Checklist","Mục yêu thích"].map((tab,index)=><button key={tab} className={`rounded-full px-5 py-3 text-sm font-extrabold ${index===0?"bg-charcoal text-champagne":"bg-white text-charcoal"}`}>{tab}</button>)}</div><div className="grid gap-6 lg:grid-cols-[280px_1fr]"><aside className="rounded-3xl bg-white p-6 shadow-soft"><h3 className="font-extrabold text-charcoal">Danh mục & thẻ</h3>{["Tất cả nội dung","Nhuộm màu","Tẩy & nâng nền","Phục hồi tóc","Kinh doanh salon","AI trong salon"].map((item)=><div key={item} className="mt-4 flex items-center justify-between text-sm font-bold text-warmgray"><span>{item}</span><span>12</span></div>)}</aside><div className="grid gap-6 md:grid-cols-2">{[...articles,...articles].map((article,index)=><ArticleCard key={`${article.slug}-${index}`} article={article}/>)}</div></div></AppShell>}
